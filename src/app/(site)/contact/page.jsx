import ContactClient from '@/components/contacPage/ContactClient';

export const metadata = {
  title: 'Contact Us | TeqCertify',
  description: 'Book your diagnostic and explore common questions.',
};

export default function ContactPage() {
  return <ContactClient />;
}
